---
import type { GetStaticPaths } from 'astro';
import { instruments } from '@/lib/data/instruments';

export const getStaticPaths: GetStaticPaths = () => {
  const locales = ['es', 'de', 'fr'] as const;
  return locales.flatMap((locale) =>
    instruments.map((i) => ({ params: { locale, instrument: i.slug } }))
  );
};

const { locale, instrument } = Astro.params;
return Astro.redirect(`/${locale}/tools/tuner/${instrument}`, 301);
---
